export default function StopButton({ onClick }) {
    return (
        
    <button onClick={onClick} className="bg-[#FF0000BF] text-white w-19 h-19 font-bold rounded-full text-center cursor-pointer">
        PARAR
    </button>

    );
}

